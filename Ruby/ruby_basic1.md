# Ruby compared with python

## 1. Ruby compared with python3

Ruby and python both have very a similar style in language.

ex) str, list indexing

### - Data Type
| section | python3 | Ruby |
| ------- | ------- | ---- |
|         |         |      |
|         |         |      |
|         |         |      |
|         |         |      |
|         |         |      |
|         |         |      |
|         |         |      |
|         |         |      |




### - Differences

|section| python3               | Ruby             |
| --------------------- | ---------------- | --------------------- |
| sdout     | print()    |puts()|
|  | print(math.ceil(2.2)) |puts(2.2.ceil())|
|                       | print(math.pi) |puts(Math::PI)|
| boolean | True |true (all lower case)|
|                       | False |false (all lower case)|
| stdio | a=input("Comments") |a=gets.chomp()|
| | ||
| | ||

### - Conditional phrase

```python
# python
if True:
    print("code1")
    print("code2")
print(print("code3"))

a=4
if a==4:
    print("code1")
elif a==3:
    print("code2")
else:
    print("code3")
```

```ruby
# Ruby
if ture  # lower case boolean, no colon.
    puts("code1") # indentation, the same as one of python 
    puts("code2")
end     # end declaration is needed!
puts("code3")

a==4

if a==4
    puts("code1")
elsif a==3
    puts("code2")
else
    puts("code3")
end
```

### - Repetitive Phrase

### - Input & Output

| section | python3          | ruby          |
| ------- | ---------------- | ------------- |
| input   | input(<comment>) | gets.chomp()  |
| output  | print(<String>)  | pus(<String>) |

### - Logical Arithmetic

same as the python!

* and
* or

### - Annotation

same as the python!

* #
* ''' '''
* """ """

### - Container (list,array, etc):  Py3 List <-> Ruby Array 

```python
#python
names=['egoing','leezche','graphittie'] 
print(type(names)) # list
print(names[0]) # egoing
```

```ruby
#Ruby
names=['egoing','leezche','graphittie']
puts(names.class) # Array
puts(names[0]) # egoing
```





## 2. Input &