# Heap
application of complete binary tree.
data structure to find nodes that have maximum or minimum keys.

1. MaxHeap
parent node key > child node key
root has maximum.

2. MinHeap
parent node key <> child node key
root has minimum.

* Priority Queue can be realiezed by Heap as well.
* 